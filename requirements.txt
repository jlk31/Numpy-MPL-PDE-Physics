# dragsweep runtime dependencies
#
# The matplotlib floor is not cosmetic: the script calls ax.grid(visible=...),
# a keyword that only exists from 3.5.0 onwards.
# https://matplotlib.org/stable/api/prev_api_changes/api_changes_3.5.0.html

numpy>=1.20
matplotlib>=3.5
